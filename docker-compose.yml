version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: stick_db
    restart: always
    env_file:
      - .env.local
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: stick_backend
    restart: always
    env_file:
      - .env.local
    ports:
      - "8000:8000"
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    container_name: stick_frontend
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    depends_on:
      - backend

volumes:
  pgdata:
